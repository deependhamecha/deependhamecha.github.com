$(document).ready(function(){
	$('.menu-background').on ('mousewheel', function (e) {
	    var delta = e.originalEvent.wheelDelta;
	    
	    if (delta < 0) {
	        
	    } else if (delta > 0) {
	        
	    }
	});
});